ifneq ($(PURPOSE),DEBUG)
	#-- undefine bound checking for non-debug compilation
	FORTRAN_BOUND_CHECK = 
endif

%_Command.o : %_Command.f90
	$(FORTRAN_COMPILE) $(DEFINES_F) $(FFLAGS) $(FORTRAN_BOUND_CHECK) $(INCLUDE) $<

%_Function.o : %_Function.f90
	$(FORTRAN_COMPILE) $(DEFINES_F) $(FFLAGS) $(FORTRAN_BOUND_CHECK) $(INCLUDE) $<

%_Kernel.o : %_Kernel.f90
	$(FORTRAN_COMPILE) $(DEFINES_F) $(FFLAGS) $(FORTRAN_BOUND_CHECK) $(INCLUDE) $<

%_Test.o : %_Test.f90
	$(FORTRAN_COMPILE) $(DEFINES_F) $(FFLAGS) $(FORTRAN_BOUND_CHECK) $(INCLUDE) $<

%.o : %.f90
	$(FORTRAN_COMPILE) $(FFLAGS) $(FORTRAN_BOUND_CHECK) $(INCLUDE) $<

%_HIP.o : %_HIP.c
	$(DEVICE_COMPILE) $(DEFINES_DEVICE) $(DEVICE_FLAGS) $(INCLUDE) $<

%_CUDA.o : %_CUDA.c
	$(DEVICE_COMPILE) $(DEFINES_DEVICE) $(DEVICE_FLAGS) $(INCLUDE) $<

%.o : %.c
	$(CC_COMPILE) $(DEFINES_C) $(CFLAGS) $(INCLUDE) $<
